{
  "name": "aws-lambda",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "esbuild bank_api.ts --bundle --minify --sourcemap --platform=node --target=es2022 --outfile=dist/bank_api.js"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/aws-lambda": "^8.10.149",
    "devmon": "^0.0.1",
    "esbuild": "^0.25.4",
    "typescript": "^5.8.3"
  }
}
